# --- [ General Settings ] ---
# Theme and Colors
include colors.conf

# Fonts
font_family      CaskaydiaCove Nerd Font Mono
bold_font        auto
italic_font      auto
bold_italic_font auto
font_size        11.5

# Visuals
window_padding_width 25
cursor_trail         1
enable_audio_bell    no
allow_remote_control yes


# --- [ Window Management ] ---
# Layout Definition (Master on left, stack on right)
enabled_layouts tall:bias=50;full_size=1;mirrored=false, stack

# Window Border Colors (Uniform Style)
window_border_width   1.0pt
draw_minimal_borders  yes

# Shortcuts: Creation and Movement
map ctrl+shift+n     new_window
map ctrl+shift+enter move_window_to_top

# Shortcuts: Vi-style Navigation (Spatial)
map ctrl+shift+h     neighboring_window left
map ctrl+shift+l     neighboring_window right
map ctrl+shift+k     neighboring_window up
map ctrl+shift+j     neighboring_window down

# Shortcuts: Tab Navigation
map shift+ctrl+i        prev_tab
map shift+ctrl+o        next_tab

# --- [ Tab Bar ] ---
# Layout and Behavior
tab_bar_style           custom
tab_bar_edge            bottom
tab_bar_align           left
tab_bar_margin_height   5.0 0.0
tab_separator           ""
tab_fade                0 0 0 0

# Tab Appearance (Simplified for use with tab_bar.py)
active_tab_font_style   bold
inactive_tab_font_style normal

# Templates: {index} shows the number, {title} shows the process name
# Note: The fire icon is now handled by your tab_bar.py script
tab_title_template          "{fmt.fg._6A698A}{fmt.bg.default}  {index}: {title} "
active_tab_title_template   "{fmt.fg._83b6af}{fmt.bg.default}  {index}: {title} "
